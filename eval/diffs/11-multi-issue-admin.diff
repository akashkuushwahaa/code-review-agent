@@ -0,0 +1,11 @@
+import os
+from flask import request
+
+ADMIN_TOKEN = "admin-9f8e7d6c5b4a3210"
+
+def run_report(conn, report_name):
+    rows = conn.execute("SELECT * FROM reports WHERE name = '" + report_name + "'")
+    return rows.fetchall()
+
+def export_logs(log_path):
+    os.system(f"tar -czf /tmp/logs.tar.gz {log_path}")
